/*
Arrays are Ordered Data Structured

Insertion O(n) -In the beginning-
Removal O(n) -In the beginning-
Searching O(n)
Access O(1)

.push() O(1)
.pop() O(1)
.shift() O(n)
.unshift() O(n)
.concat() O(n)
.slice() O(n)
.splice() O(n)
.sort() O(n* logN)
.forEach() / .map() / .filter() / .reduce() O(n)
*/